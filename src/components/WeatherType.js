import React from 'react';

const WeatherType = (weatherType) => {

    return(
        <div className="weather">
            {weatherType}
        </div>
    );
}

export default WeatherType;